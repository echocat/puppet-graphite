fixtures:
  repositories:
    stdlib: 
      repo: "git://github.com/puppetlabs/puppetlabs-stdlib.git"
      ref: "4.2.0"
    logrotate:
      repo: "git://github.com/rodjek/puppet-logrotate.git"
      ref: "1.1.1"
    python:
      repo: "git://github.com/stankevich/puppet-python.git"
      ref: "1.8.2"
  symlinks:
    graphite: "#{source_dir}"
